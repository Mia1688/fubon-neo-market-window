# 看盤視窗使用手冊

版本：v1.0.0  
適用系統：Windows 10／11 64 位元  
專案網址：https://github.com/Mia1688/fubon-neo-market-window

## 明天到公司前先準備

請不要只依賴公司網路能開 GitHub，今天先把下列檔案準備好：

1. 下載程式 ZIP：  
   https://github.com/Mia1688/fubon-neo-market-window/archive/refs/heads/main.zip
2. 把 ZIP 存到公司電腦可取得的位置，例如自己的隨身碟或合規的雲端空間。
3. 另外準備自己的富邦 PFX 憑證檔與憑證密碼。不要把 PFX 放進 GitHub、Email 公開附件或傳給其他人。
4. 確認知道富邦登入帳號與登入密碼。

## 系統需求

- Windows 10／11 64 位元。本程式使用 Windows DPAPI 保存憑證密碼，不支援 macOS。
- Python 3.11～3.13 64 位元。
- 可連線至 GitHub/Python 套件來源（僅第一次安裝需要），以及富邦 Neo、行情、證交所與元大 ETF 資料服務。
- 富邦證券正式帳號、登入密碼、有效的 PFX 憑證及憑證密碼。

Python 套件版本：

- `fubon-neo==2.2.8`
- `PySide6>=6.8,<7`

## 第一次安裝

1. 將下載的 ZIP 解壓縮。不可直接在 ZIP 預覽畫面中執行。
2. 打開解壓縮後的 `fubon-neo-market-window-main` 資料夾。
3. 雙擊 `install.bat`。
4. 等到視窗顯示 `Installation completed`，再按任意鍵關閉。
5. 雙擊 `start.bat` 啟動看盤視窗。

如果公司禁止執行 `.bat`，可在該資料夾的網址列輸入 `cmd` 後按 Enter，再依序輸入：

```bat
py -3 -m pip install --user -r requirements.txt
py -3 app.py
```

## 第一次登入與憑證

1. 在上方登入列輸入富邦帳號與登入密碼。
2. 按「登入並連線」。
3. 第一次使用會出現「一次性憑證設定」，選取自己的 `.pfx` 憑證。
4. 輸入 PFX 的憑證密碼。
5. 登入成功後，右上狀態會顯示 `production · 已登入`。

登入密碼不會寫入檔案。憑證路徑及由 Windows DPAPI 加密的憑證密碼保存在本機 `settings.json`，只能由同一台 Windows 電腦的同一位使用者解密。

如果把整個資料夾搬到另一台電腦，請刪除新電腦上的 `settings.json`，再重新選擇 PFX 並輸入憑證密碼。

## 看盤視窗

- 左上「庫存持股」：顯示庫存商品、即時價格、漲跌幅、庫存及可賣數量。
- 右上頁籤：切換台指期與發行量加權股價指數。
- 左下「快速下單」：股票現股或台指期、買賣方向、限價或市價。
- 左下「預掛查詢」：查看本機預掛，勾選後可取消尚未送出的項目。
- 右下「歷史日 K」：輸入股票代碼後按「載入」。

四個區塊之間的分隔線可用滑鼠拖曳調整大小。

## 一般下單

1. 選擇「股票現股」或「台指期貨」。
2. 選擇買進或賣出。
3. 選擇限價或市價；限價單必須填價格。
4. 輸入商品代碼與數量。
5. 按送出後再次核對確認視窗。

注意：股票下單數量欄以「股」為單位；台指期以「口」為單位。市價單不保證成交價格，正式送單前務必再次確認商品、方向與數量。

## 本機預掛

- 預掛只支援股票限價 ROD 單，不支援台指期及市價單。
- 加入預掛時，委託尚未送到富邦，不會出現在券商委託查詢。
- 程式會安排下一個工作日 08:31 送出。
- 到時間時程式必須保持開啟並成功登入；否則會等下次登入再處理。
- 「預掛查詢」可勾選並取消尚未送出的本機預掛。
- 券商已受理的委託必須到正式券商委託查詢確認或刪單，不能只刪除本機記錄。

## 漲停監控與追單

1. 登入後切換左側「漲停監控」。
2. 選擇股票分類。
3. 設定距漲停 tick 數、最低成交量（張）及追單數量（張）。
4. 按「立即掃描」，或等待每 30 秒自動更新。
5. 可按每列「市價買進」，或先選取一列後按「市價追選取標的」。
6. 確認視窗會同時顯示張數與換算股數；一般股票通常每張為 1,000 股。

頁面最下方的「程式執行訊息」會保留最近一次掃描或追單結果。若券商拒單，請完整抄下這一欄的原因。

## 打不開時的快速排查

### 雙擊 start.bat 完全沒反應

1. 打開程式資料夾。
2. 點檔案總管上方網址列，輸入 `cmd` 後按 Enter。
3. 輸入：

```bat
py -3 app.py
```

不要直接關閉黑色視窗；把最後 20 行錯誤訊息截圖保存。

### 顯示找不到 py、python 或不是內部命令

代表 Python 尚未安裝，或安裝時沒有加入 PATH。安裝 Python 3.11～3.13 64 位元後重新開機，再執行 `install.bat`。

可用下列指令確認：

```bat
py -3 --version
```

### 顯示 ModuleNotFoundError

在程式資料夾執行：

```bat
py -3 -m pip install --user -r requirements.txt
py -3 -m pip show fubon-neo PySide6
```

第二行應顯示兩個套件的安裝資訊。

### pip 無法下載或 SSL／Proxy 錯誤

通常是公司 Proxy、防火牆或憑證檢查阻擋 Python 套件下載。請把完整錯誤交給公司 IT，請 IT 允許 Python/pip 透過 HTTPS 連線。不要自行關閉 SSL 驗證。

如果今天已在可上網的 Windows 電腦安裝成功，最保險的做法是直接使用該台已安裝完成的電腦；單純複製程式 ZIP 不會連同 Python 套件一起搬過去。

### 登入顯示 certificate key error

1. 關閉程式。
2. 刪除程式資料夾中的 `settings.json`。
3. 重新啟動，重新選擇正確 PFX 並輸入憑證密碼。

不要刪除 `pending_orders.json`，除非確定不需要其中的本機預掛資料。

### 登入失敗但憑證正確

依序確認：

- 富邦帳號及登入密碼是否正確。
- PFX 是否屬於該帳號且仍有效。
- 電腦日期、時間與時區是否正確。
- 公司網路是否允許 HTTPS 與 WebSocket（WSS）對外連線。
- VPN、Proxy 或端點防護是否攔截富邦 Neo SDK。

### 登入成功但沒有行情

- 確認右上角顯示已登入。
- 按「重新整理」。
- 非交易時段可能沒有即時成交更新，但歷史日 K 應仍可查詢。
- 若只有部分資料失敗，查看底部狀態及漲停監控的「程式執行訊息」。
- 將錯誤文字、發生時間及畫面截圖交給維護人員。

### GitHub 在公司被封鎖

使用事先下載保存的 ZIP；不要到公司後才第一次下載。若公司政策禁止使用隨身碟或個人雲端，請提前請 IT 提供核准的檔案傳輸方式。

## 問題回報需要提供什麼

請一次提供：

1. 發生時間。
2. 操作到哪一步。
3. 完整錯誤文字，不要只寫「不能用」。
4. 錯誤畫面截圖。
5. `py -3 --version` 的輸出。
6. `py -3 -m pip show fubon-neo PySide6` 的輸出。

請遮住帳號、登入密碼、憑證密碼、PFX 路徑及委託書號後再傳送截圖。不要傳送 PFX 憑證檔。

## 下班前確認清單

- [ ] ZIP 已下載並可解壓縮。
- [ ] Python 已安裝。
- [ ] `install.bat` 已顯示成功。
- [ ] `start.bat` 可開啟視窗。
- [ ] PFX 憑證及密碼已安全準備。
- [ ] 已能登入並看到庫存或行情。
- [ ] 已用小額或不送單方式熟悉確認畫面。


