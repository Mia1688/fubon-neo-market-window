# 看盤視窗使用手冊

版本：v1.1.0 Windows 免安裝版  
適用系統：Windows 10／11 64 位元  
專案網址：https://github.com/Mia1688/fubon-neo-market-window

## EXE 下載

最新版下載頁：  
https://github.com/Mia1688/fubon-neo-market-window/releases/latest

Windows 免安裝版直接下載：  
https://github.com/Mia1688/fubon-neo-market-window/releases/latest/download/FubonNeoMarketWindow-v1.1.0-win64.zip

下載後請核對 Release 頁面提供的 SHA-256，再交由公司 IT 檢查或放行。

## 系統需求

- Windows 10／11 64 位元。本程式使用 Windows DPAPI 保存憑證密碼，不支援 macOS。
- EXE 版已包含 Python、富邦 Neo SDK 與 PySide6，不需另外安裝 Python，也不需執行 BAT。
- 執行時需連線至富邦 Neo、行情、證交所與元大 ETF 資料服務。
- 富邦證券正式帳號、登入密碼、有效的 PFX 憑證及憑證密碼。

打包內含套件版本：

- `fubon-neo==2.2.8`
- `PySide6>=6.8,<7`

## 第一次開啟 EXE

1. 下載 `FubonNeoMarketWindow-v1.1.0-win64.zip`。
2. 在 ZIP 上按滑鼠右鍵，選「全部解壓縮」。不可直接在 ZIP 預覽畫面執行。
3. 建議解壓縮到簡短路徑，例如 `C:\MarketWindow`。
4. 打開 `FubonNeoMarketWindow` 資料夾。
5. 雙擊 `FubonNeoMarketWindow.exe`。

`FubonNeoMarketWindow.exe` 旁邊的 `_internal` 資料夾是程式的一部分。不可只複製 EXE，也不可刪除或更名 `_internal`，否則程式無法啟動。

本版本不會執行 `install.bat` 或 `start.bat`。原始碼版仍保留給開發與稽核使用，但公司電腦請優先使用 EXE 免安裝版。

## 第一次登入與憑證

1. 在上方登入列輸入富邦帳號與登入密碼。
2. 按「登入並連線」。
3. 第一次使用會出現「一次性憑證設定」，選取自己的 `.pfx` 憑證。
4. 輸入 PFX 的憑證密碼。
5. 登入成功後，右上狀態會顯示 `production · 已登入`。

登入密碼不會寫入檔案。憑證路徑及由 Windows DPAPI 加密的憑證密碼保存在本機 `settings.json`，只能由同一台 Windows 電腦的同一位使用者解密。

如果把整個資料夾搬到另一台電腦，請刪除新電腦上的 `settings.json`，再重新選擇 PFX 並輸入憑證密碼。

## 看盤視窗

- 左上「庫存持股」：顯示庫存商品、即時價格、漲跌幅、庫存及可賣數量。
- 右上頁籤：切換台指期與發行量加權股價指數。
- 左下「快速下單」：股票現股或台指期、買賣方向、限價或市價。
- 左下「預掛查詢」：查看本機預掛，勾選後可取消尚未送出的項目。
- 右下「歷史日 K」：輸入股票代碼後按「載入」。

四個區塊之間的分隔線可用滑鼠拖曳調整大小。

## 一般下單

1. 選擇「股票現股」或「台指期貨」。
2. 選擇買進或賣出。
3. 選擇限價或市價；限價單必須填價格。
4. 輸入商品代碼與數量。
5. 按送出後再次核對確認視窗。

注意：股票下單數量欄以「股」為單位；台指期以「口」為單位。市價單不保證成交價格，正式送單前務必再次確認商品、方向與數量。

## 本機預掛

- 預掛只支援股票限價 ROD 單，不支援台指期及市價單。
- 加入預掛時，委託尚未送到富邦，不會出現在券商委託查詢。
- 程式會安排下一個工作日 08:31 送出。
- 到時間時程式必須保持開啟並成功登入；否則會等下次登入再處理。
- 「預掛查詢」可勾選並取消尚未送出的本機預掛。
- 券商已受理的委託必須到正式券商委託查詢確認或刪單，不能只刪除本機記錄。

## 漲停監控與追單

1. 登入後切換左側「漲停監控」。
2. 選擇股票分類。
3. 設定距漲停 tick 數、最低成交量（張）及追單數量（張）。
4. 按「立即掃描」，或等待每 30 秒自動更新。
5. 可按每列「市價買進」，或先選取一列後按「市價追選取標的」。
6. 確認視窗會同時顯示張數與換算股數；一般股票通常每張為 1,000 股。

頁面最下方的「程式執行訊息」會保留最近一次掃描或追單結果。若券商拒單，請完整抄下這一欄的原因。

## 打不開時的快速排查

### Windows SmartScreen 顯示「已保護您的電腦」

此 EXE 沒有商業程式碼簽章，因此 Windows 可能第一次出現警告。先確認檔案來自本手冊的 GitHub Release，並核對 SHA-256。若公司政策允許，可按「其他資訊」後選「仍要執行」。

若畫面顯示「系統管理員已封鎖」或公司使用 AppLocker／EDR，請把 GitHub Release 網址、SHA-256 與本手冊交給 IT 放行。不要關閉防毒、不要變更公司安全設定，也不要嘗試繞過管制。

### 雙擊 EXE 完全沒反應

依序確認：

- ZIP 是否已「全部解壓縮」。
- EXE 是否仍與 `_internal` 在同一資料夾。
- 路徑是否過長；可移到 `C:\MarketWindow` 再試。
- 檔案內容是否被公司防毒隔離。
- Windows 事件檢視器的「Windows 記錄 → 應用程式」是否有同時間錯誤。

把事件檢視器錯誤截圖及 Release 的 SHA-256 一起交給 IT 或維護人員。

### 顯示找不到 DLL、Qt 或 Python

幾乎都是只複製 EXE、沒有帶 `_internal`，或解壓縮不完整。刪除不完整的資料夾，重新下載 ZIP 並使用「全部解壓縮」。

若仍顯示缺少 Microsoft Visual C++ Runtime，請由公司 IT 安裝 Microsoft Visual C++ 2015–2022 Redistributable x64。

### 公司禁止未知或未簽章 EXE

這是公司資訊安全政策，不是改副檔名或重新壓縮可以解決。必須請 IT 核准檔案雜湊或提供公司簽章。不要嘗試繞過管制。

### 登入顯示 certificate key error

1. 關閉程式。
2. 刪除程式資料夾中的 `settings.json`。
3. 重新啟動，重新選擇正確 PFX 並輸入憑證密碼。

不要刪除 `pending_orders.json`，除非確定不需要其中的本機預掛資料。

### 登入失敗但憑證正確

依序確認：

- 富邦帳號及登入密碼是否正確。
- PFX 是否屬於該帳號且仍有效。
- 電腦日期、時間與時區是否正確。
- 公司網路是否允許 HTTPS 與 WebSocket（WSS）對外連線。
- VPN、Proxy 或端點防護是否攔截富邦 Neo SDK。

### 登入成功但沒有行情

- 確認右上角顯示已登入。
- 按「重新整理」。
- 非交易時段可能沒有即時成交更新，但歷史日 K 應仍可查詢。
- 若只有部分資料失敗，查看底部狀態及漲停監控的「程式執行訊息」。
- 將錯誤文字、發生時間及畫面截圖交給維護人員。

### GitHub 在公司被封鎖

使用事先下載保存的 ZIP；不要到公司後才第一次下載。若公司政策禁止使用隨身碟或個人雲端，請提前請 IT 提供核准的檔案傳輸方式。

## 問題回報需要提供什麼

請一次提供：

1. 發生時間。
2. 操作到哪一步。
3. 完整錯誤文字，不要只寫「不能用」。
4. 錯誤畫面截圖。
5. 使用的 Release 版本與 ZIP SHA-256。
6. Windows 版本，以及公司防毒／AppLocker 顯示的完整訊息。

請遮住帳號、登入密碼、憑證密碼、PFX 路徑及委託書號後再傳送截圖。不要傳送 PFX 憑證檔。

## 下班前確認清單

- [ ] ZIP 已下載並可解壓縮。
- [ ] `FubonNeoMarketWindow.exe` 與 `_internal` 保持在同一資料夾。
- [ ] `FubonNeoMarketWindow.exe` 可開啟視窗。
- [ ] PFX 憑證及密碼已安全準備。
- [ ] 已能登入並看到庫存或行情。
- [ ] 已用小額或不送單方式熟悉確認畫面。

